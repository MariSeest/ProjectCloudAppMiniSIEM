apiVersion: v1
kind: Service
metadata:
  name: minisiem-postgres
  namespace: group-5
  labels:
    app: minisiem-postgres
spec:
  selector:
    app: minisiem-postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP