{{- if .Values.siemService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: siem-service
  namespace: {{ .Values.namespace }}
  labels:
    app: siem-service
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: {{ .Values.siemService.replicas }}
  selector:
    matchLabels:
      app: siem-service
  template:
    metadata:
      labels:
        app: siem-service
    spec:
      containers:
        - name: siem-service
          image: "{{ .Values.siemService.image.repository }}:{{ .Values.siemService.image.tag }}"
          imagePullPolicy: {{ .Values.siemService.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.siemService.port }}
          env:
            - name: DB_URL
              value: {{ .Values.siemService.env.dbUrl }}
            - name: DB_USERNAME
              value: {{ .Values.siemService.env.dbUsername }}
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: db-password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secrets.name }}
                  key: jwt-secret
          resources:
            {{- toYaml .Values.siemService.resources | nindent 12 }}
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: {{ .Values.siemService.port }}
            initialDelaySeconds: 60
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: {{ .Values.siemService.port }}
            initialDelaySeconds: 90
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: siem-service
  namespace: {{ .Values.namespace }}
  labels:
    app: siem-service
spec:
  selector:
    app: siem-service
  ports:
    - port: {{ .Values.siemService.port }}
      targetPort: {{ .Values.siemService.port }}
  type: ClusterIP
{{- end }}
