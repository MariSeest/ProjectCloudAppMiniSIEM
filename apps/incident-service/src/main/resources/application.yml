server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/minisiem?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: minisiem
    password: minisiem
  jpa:
    hibernate:
      ddl-auto: update

management:
  endpoints:
    web:
      exposure:
        include: health,info
